// Copy to runtime-config.js for production/Netlify and set your public API URL.
// The API must run on a VPS/cloud server because Netlify cannot host RTMP.
window.LW_BROADCAST_SERVER_URL = 'https://broadcast-api.yourdomain.com';
